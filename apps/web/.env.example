# Browser → main API base URL. Inlined at `pnpm run build` (Next.js NEXT_PUBLIC_*).
# Docker: set the same value as compose build-arg `NEXT_PUBLIC_BASE_URL` (root `.env.example`).
# Use a URL the user's browser can reach (e.g. http://127.0.0.1:8000 when API is published on the host).
#
# Custom domain / HTTPS: use the public API origin, e.g. https://api.example.com (no trailing slash).
# In Docker, set that in the root `.env` and rebuild the `web` image; see root README "Custom URL or domain".
NEXT_PUBLIC_BASE_URL=http://127.0.0.1:8000

# API path prefix shown in SDK config hints on the API tokens page (default /api).
# NEXT_PUBLIC_API_PREFIX=/api

# Set this to the URL of the API server. This is used to make requests to the API server internally.
SERVER_API_BASE_URL=http://127.0.0.1:8000