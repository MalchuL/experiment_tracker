"""Main-backend MLTools orchestration bounded context.

The package owns authorization and public API behavior while the separate
MLTools service owns job persistence, asynchronous execution, and results.
"""
