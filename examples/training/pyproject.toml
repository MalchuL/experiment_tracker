[project]
name = "experiment-tracker-training"
version = "0.1.0"
description = "SDK training example"
requires-python = ">=3.10"
dependencies = [
  "experiment-tracker-sdk",
  "httpx>=0.27.0",
  "numpy>=2.2.6",
  "pillow>=12.1.1",
]

[tool.uv.sources]
experiment-tracker-sdk = { path = "../../python/sdk" }
