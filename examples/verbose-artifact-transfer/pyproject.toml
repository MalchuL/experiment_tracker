[project]
name = "experiment-tracker-verbose-artifact-transfer"
version = "0.1.0"
description = "SDK example: large artifact upload/download with tqdm progress"
requires-python = ">=3.10"
dependencies = [
  "experiment-tracker-sdk",
  "tqdm>=4.66.0",
]

[tool.uv.sources]
experiment-tracker-sdk = { path = "../../python/sdk" }
